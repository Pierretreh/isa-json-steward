[flake8]
max-line-length = 100
extend-ignore =
    # E203: whitespace before ':' — conflicts with black's formatting of slices
    E203,
    # W503: line break before binary operator — conflicts with black
    W503
